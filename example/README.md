# Example Usage of Aptos-Vault 

This module shows you an example usage of [Aptos-vault](https://github.com/valekar/aptos-vault) module in your module.

The `example.move` file contains both `deposit` and `withdraw` examples